---
title: "Dimensionnement et diagnostic d'un réseau AEP en régime permanent"
subtitle: "Du graphe hydraulique au modèle opérationnel"
date: "2026-05-17"
author: ""
categories: [Hydraulique, AEP, Modélisation, Ingénierie]
page-layout: article
toc: true
toc-title: "Sommaire"
toc-depth: 3
number-sections: true
lang: fr
format:
  html:
    embed-resources: true
    html-math-method: katex
    theme: cosmo
execute:
  echo: false
  warning: false
  fig-format: svg
  fig-responsive: true
  fig-width: 8
  fig-height: 4.5
jupyter: python3
---

::: {.callout-note}
## Note de la série
Cette note fait partie d'une série sur la modélisation des réseaux hydrauliques. Elle suppose la lecture de la note **0.1** (*Généralités sur la modélisation*) et de la note **1.A** (*Modéliser un réseau AEP — spécificités et enjeux métier*). On entre ici dans la **mécanique concrète** de la résolution hydraulique en régime permanent : cette note opérationnalise les fondements (0.1 pour la cascade/Newton-Raphson, 0.2 pour l'énergie, 1.A pour le contexte métier) et ne les redéveloppe pas.
:::

**Mini-réseau fil rouge.** Toutes les figures de code de cette note s'appuient sur le même réseau exemple : 5 nœuds de jonction (N1–N5), 1 réservoir (R0, charge imposée $H_0 = 50$ m), 1 pompe (arc P sur N1–N2), 7 conduites, 1 maille fermée.

---

## Du terrain au graphe : formaliser le réseau

### Nœuds et arcs : le vocabulaire de la théorie des graphes

Avant de résoudre quoi que ce soit, un réseau AEP doit être **abstrait en un graphe mathématique**. La topologie maillée et ses conséquences (non-linéarité, redondance) ont été introduites qualitativement en note **1.A** ; cette note en donne la résolution concrète.

| Élément physique | Représentation dans le graphe |
|---|---|
| Jonction, point de livraison | Nœud avec demande $d_i$ (débit soutiré) |
| Réservoir, château d'eau | Nœud à charge imposée $H_i = \text{cste}$ |
| Conduite | Arc avec relation perte de charge $\Delta H_{ij}(Q_{ij})$ |
| Pompe | Arc avec relation énergie ajoutée $\Delta H_{ij} = -f_{pompe}(Q_{ij})$ |
| Vanne, PRV | Arc avec condition aux limites interne |

: Correspondance entre éléments physiques et représentation dans le graphe {#tbl-graphe}

```{python}
#| label: fig-mini-reseau
#| fig-cap: "Mini-réseau fil rouge utilisé dans toutes les figures de code de cette note : 5 nœuds de jonction (N1–N5), 1 réservoir R0 à charge imposée, 1 pompe (arc P, N1→N2), 7 conduites et 1 maille fermée (N2–N3–N4–N5). La demande nodale $d_i$ est indiquée en rouge ; la résistance hydraulique $r_{ij}$ en gris sur chaque arc. Ce graphe est volontairement minimal pour illustrer tous les mécanismes (non-linéarité, loi des mailles, point de fonctionnement) sans surcharger les figures."
import numpy as np
import matplotlib.pyplot as plt
import matplotlib.patches as mpatches
from matplotlib.patches import FancyArrowPatch, FancyBboxPatch

C_CHARGE = "#1f4e79"
C_PIEZO  = "#2e8b57"
C_ACCENT = "#c0392b"

fig, ax = plt.subplots(figsize=(8, 5.0))
ax.set_xlim(-0.5, 5.5); ax.set_ylim(-0.5, 4.5); ax.axis("off")

# Positions des nœuds
pos = {"R0": (0.0, 2.5),
       "N1": (1.5, 2.5),
       "N2": (3.0, 3.5),
       "N3": (4.5, 3.5),
       "N4": (4.5, 1.5),
       "N5": (3.0, 1.5)}

demandes = {"N1": 0, "N2": 5, "N3": 8, "N4": 6, "N5": 7}
charges  = {"R0": "H₀=50 m (imposée)"}

# Arêtes : (from, to, label_r, is_pump)
arcs = [
    ("R0", "N1", "$r_{01}$", False),
    ("N1", "N2", "Pompe P",  True),
    ("N1", "N5", "$r_{15}$", False),
    ("N2", "N3", "$r_{23}$", False),
    ("N3", "N4", "$r_{34}$", False),
    ("N4", "N5", "$r_{45}$", False),
    ("N5", "N2", "$r_{52}$", False),
]

# Dessiner les arêtes
for (u, v, lbl, is_pump) in arcs:
    x0, y0 = pos[u]; x1, y1 = pos[v]
    col = "#e67e22" if is_pump else "#888"
    lw  = 2.5 if is_pump else 1.8
    ax.annotate("", xy=(x1,y1), xytext=(x0,y0),
                arrowprops=dict(arrowstyle="->", color=col, lw=lw))
    mx, my = (x0+x1)/2, (y0+y1)/2
    dy = 0.18 if y1 >= y0 else -0.18
    ax.text(mx, my+dy, lbl, ha="center", fontsize=8,
            color="#e67e22" if is_pump else "#555")

# Dessiner les nœuds
for name, (x, y) in pos.items():
    if name == "R0":
        b = FancyBboxPatch((x-0.35, y-0.30), 0.70, 0.60,
            boxstyle="round,pad=0.05", fc="#d6eaf8", ec=C_CHARGE, lw=2)
        ax.add_patch(b)
        ax.text(x, y+0.08, "R0", ha="center", fontsize=9, color=C_CHARGE, fontweight="bold")
        ax.text(x, y-0.18, "$H_0=50$ m", ha="center", fontsize=7.5, color=C_CHARGE)
    else:
        d = demandes[name]
        ax.scatter([x],[y], s=400, color=C_CHARGE, zorder=5)
        ax.text(x, y+0.08, name, ha="center", fontsize=9, color="white", fontweight="bold",
                zorder=6)
        ax.text(x, y-0.32, f"$d={d}$ m³/h", ha="center", fontsize=8, color=C_ACCENT)

# Maille
ax.annotate("", xy=(4.5+0.02, 2.5), xytext=(4.5+0.02, 2.5),
            arrowprops=dict(arrowstyle="-"))
ax.text(4.1, 2.5, "Maille\n$\\sum\\Delta H=0$", ha="center", fontsize=8,
        color=C_PIEZO, style="italic",
        bbox=dict(boxstyle="round,pad=0.2", fc="#eafaf1", ec=C_PIEZO, alpha=0.8))

ax.set_title("Mini-réseau fil rouge — Graphe hydraulique annoté", fontsize=10.5)
fig.tight_layout()
plt.show()
```

### Les deux lois de Kirchhoff hydraulique

La résolution du réseau repose sur deux principes de conservation — la forme discrète de la continuité de Navier-Stokes (note **0.1**) et la conservation de l'énergie de la note **0.2** appliquée à un cycle fermé :

**Loi des nœuds — conservation de la masse :**

$$\sum_{j \in \mathcal{V}(i)} Q_{ij} - d_i = 0 \quad \forall i \in \{1, \ldots, N\}$$

**Loi des mailles — conservation de l'énergie :**

$$\sum_{(i,j) \in \mathcal{M}} \Delta H_{ij} = 0$$

```{python}
#| label: fig-kirchhoff
#| fig-cap: "Illustration des deux lois de Kirchhoff hydraulique sur le mini-réseau fil rouge. Gauche : loi des nœuds au nœud N5 — la somme algébrique des débits entrants ($Q_{15}$, $Q_{45}$) et sortants ($Q_{52}$, $d_5$) est nulle. Droite : loi des mailles sur la maille N2–N3–N4–N5 — la somme des pertes de charge sur le cycle est nulle (les flèches de couleur indiquent le signe : perte positive dans le sens de parcours, négative en sens inverse)."
import numpy as np
import matplotlib.pyplot as plt
from matplotlib.patches import FancyArrowPatch

C_CHARGE = "#1f4e79"
C_PIEZO  = "#2e8b57"
C_ACCENT = "#c0392b"

fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(10, 4.5))

# --- Loi des nœuds N5 ---
ax1.set_xlim(-2.5, 2.5); ax1.set_ylim(-2.5, 2.5); ax1.axis("off")
ax1.scatter([0],[0], s=600, color=C_CHARGE, zorder=5)
ax1.text(0, 0, "N5", ha="center", va="center", fontsize=10, color="white", fontweight="bold")

arrows_noeud = [
    ((-2.0, 0.8), (-0.25, 0.12), "$Q_{15}$ (entrant)", C_PIEZO),
    ((-0.5, -2.0), (-0.08, -0.28), "$Q_{45}$ (entrant)", C_PIEZO),
    ((0.28, 0.12), (2.0, 0.8),    "$Q_{52}$ (sortant)", C_ACCENT),
    ((0.0, -0.32), (0.0, -2.0),   "$d_5 = 7$ m³/h\n(soutirage)", C_ACCENT),
]
for (x0,y0),(x1,y1),lbl,col in arrows_noeud:
    ax1.annotate("", xy=(x1,y1), xytext=(x0,y0),
                 arrowprops=dict(arrowstyle="->", color=col, lw=2.2))
    ax1.text((x0+x1)/2, (y0+y1)/2 + 0.2, lbl, ha="center", fontsize=8.5, color=col)

ax1.text(0, 2.3, r"$Q_{15} + Q_{45} - Q_{52} - d_5 = 0$",
         ha="center", fontsize=9, color="#333",
         bbox=dict(boxstyle="round,pad=0.3", fc="#f0f0f0", ec="#aaa"))
ax1.set_title("Loi des nœuds — Nœud N5", fontsize=10)

# --- Loi des mailles N2-N3-N4-N5 ---
ax2.set_xlim(-0.5, 4.0); ax2.set_ylim(-0.5, 3.5); ax2.axis("off")
pos_m = {"N2": (0.5, 2.5), "N3": (3.5, 2.5), "N4": (3.5, 0.5), "N5": (0.5, 0.5)}
for n, (x,y) in pos_m.items():
    ax2.scatter([x],[y], s=450, color=C_CHARGE, zorder=5)
    ax2.text(x, y, n, ha="center", va="center", fontsize=9.5, color="white", fontweight="bold")

maille_arcs = [
    ("N2","N3","$+\\Delta H_{23}$", C_PIEZO),
    ("N3","N4","$+\\Delta H_{34}$", C_PIEZO),
    ("N4","N5","$-\\Delta H_{45}$", C_ACCENT),
    ("N5","N2","$-\\Delta H_{52}$", C_ACCENT),
]
for (u,v,lbl,col) in maille_arcs:
    x0,y0 = pos_m[u]; x1,y1 = pos_m[v]
    ax2.annotate("", xy=(x1,y1), xytext=(x0,y0),
                 arrowprops=dict(arrowstyle="->", color=col, lw=2.2))
    ax2.text((x0+x1)/2 + (0.3 if x1>x0 else -0.3),
             (y0+y1)/2 + (0.2 if y1>y0 else -0.2),
             lbl, ha="center", fontsize=8.5, color=col)

ax2.text(2.0, 1.5, r"$\sum_{maille}\Delta H_{ij} = 0$",
         ha="center", fontsize=10, color="#333",
         bbox=dict(boxstyle="round,pad=0.3", fc="#f0f0f0", ec="#aaa"))
ax2.set_title("Loi des mailles — Maille N2–N3–N4–N5", fontsize=10)

fig.tight_layout()
plt.show()
```

### Pourquoi le système est non linéaire

La relation entre perte de charge et débit est **non linéaire** :

$$\Delta H_{ij} = r_{ij} \cdot |Q_{ij}|^{n-1} \cdot Q_{ij}$$

Les résistances $r_{ij}$ et l'exposant $n$ découlent des lois de perte de charge fondées dimensionnellement en note **0.4** (Moody, Colebrook). $n = 2$ pour Darcy-Weisbach, $n \approx 1{,}85$ pour Hazen-Williams.

```{python}
#| label: fig-nonlinearite
#| fig-cap: "Non-linéarité de la relation perte de charge–débit pour Darcy-Weisbach ($n=2$) et Hazen-Williams ($n=1{,}85$), sur une conduite DN 150, $L=500$ m. L'inversion directe (résolution sans itération) est impossible : la pente $d(\\Delta H)/dQ$ varie continûment avec le débit. La conductance hydraulique $a_{ij} = n r_{ij}|Q|^{n-1}$ (tangente à la courbe) est recalculée à chaque itération de Newton-Raphson — c'est le principe de la méthode du gradient hydraulique."
import numpy as np
import matplotlib.pyplot as plt

C_CHARGE = "#1f4e79"
C_PIEZO  = "#2e8b57"
C_ACCENT = "#c0392b"

Q = np.linspace(0, 100, 300)  # m3/h
D = 0.15; L = 500; rho = 1000; nu = 1e-6; g = 9.81
A = np.pi*D**2/4
V = Q/3600/A
Re = np.maximum(V*D/nu, 1)
f = (1/(-2*np.log10(0.0001/D/3.7 + 2.51/(Re*0.02**0.5))))**2
r_DW = f * L / D / (2*g*A**2 * 3600**2)
dH_DW = r_DW * Q**2

# Hazen-Williams (C_HW=130)
C_HW = 130;
r_HW = L / (C_HW**1.852 * D**4.87 * 0.2785)
dH_HW = r_HW * Q**1.852

fig, ax = plt.subplots(figsize=(7.5, 4.2))
ax.plot(Q, dH_DW, color=C_CHARGE, lw=2.5, label=f"Darcy-Weisbach ($n=2$)")
ax.plot(Q, dH_HW, color=C_PIEZO,  lw=2.2, ls="--", label="Hazen-Williams ($n=1{,}85$)")

# --- CORRECTION : Calcul des valeurs scalaires strictes au point Q0 = 60 ---
Q0 = 60
V0 = Q0/3600/A
Re0 = np.maximum(V0*D/nu, 1)
f0 = (1/(-2*np.log10(0.0001/D/3.7 + 2.51/(Re0*0.02**0.5))))**2
r_DW_0 = f0 * L / D / (2*g*A**2 * 3600**2)

dH0_DW = r_DW_0 * Q0**2
slope_DW = 2 * r_DW_0 * Q0
# -------------------------------------------------------------------------

ax.plot([Q0-20, Q0+20], [dH0_DW - slope_DW*20, dH0_DW + slope_DW*20],
        color=C_ACCENT, lw=1.5, ls=":", label=f"Tangente (conductance $a_{{ij}}$ à $Q={Q0}$ m³/h)")
ax.scatter([Q0], [dH0_DW], color=C_ACCENT, s=70, zorder=6)

ax.set_xlabel("Débit $Q$ (m³/h)", fontsize=9)
ax.set_ylabel("Perte de charge $\\Delta H$ (m)", fontsize=9)
ax.set_title("Non-linéarité $\\Delta H(Q)$ — DN 150, 500 m", fontsize=10)
ax.legend(fontsize=8.5)
ax.set_xlim(0, 100);
ax.set_ylim(0, None)
ax.grid(alpha=0.2); ax.tick_params(labelsize=8.5)
fig.tight_layout()
plt.show()
```

---

## La méthode du gradient hydraulique

### Principe général

La méthode du gradient hydraulique (Todini et Pilati, 1987 — implémentée dans EPANET) reformule le problème en termes de **charges nodales** $H_i$. C'est l'application concrète de la méthode évoquée en note **0.1** (section « méthodes de résolution en charge »). On part d'une solution initiale $\mathbf{H}^{(0)}$ et on itère selon Newton-Raphson :

$$\mathbf{H}^{(k+1)} = \mathbf{H}^{(k)} - \left[\mathbf{J}^{(k)}\right]^{-1} \cdot \mathbf{F}\left(\mathbf{H}^{(k)}\right)$$

où $\mathbf{F}(\mathbf{H})$ est le vecteur des résidus de bilan de masse et $\mathbf{J}$ la jacobienne.

::: {.callout-note}
## Le coin du puriste — Structure du solveur Newton-Raphson minimal

La structure d'un solveur Newton-Raphson pour le mini-réseau fil rouge (esquisse Python pédagogique — implémentation industrielle : WNTR/EPANET) :

```python
import numpy as np

# Données du mini-réseau (5 nœuds inconnus, résistances r_ij)
# H_ref = 50.0  # charge imposée au réservoir R0

def debits(H, H_ref, r, n=2):
    """Calcule les débits dans chaque arc à partir des charges nodales."""
    # Pour chaque arc (i,j) : Q_ij = sign(H_i-H_j) * |H_i-H_j|^(1/n) / r^(1/n)
    ...

def residus(H, H_ref, demandes, r, n=2):
    """Vecteur F(H) : bilan de masse à chaque nœud (loi des nœuds)."""
    Q = debits(H, H_ref, r, n)
    # F_i = sum_j Q_ij - d_i
    ...
    return F

def jacobienne(H, H_ref, r, n=2):
    """Matrice J = dF/dH (conductances hydrauliques)."""
    # J_ii = sum_j n*r_ij*|Q_ij|^(n-1)   (diagonale)
    # J_ij = -n*r_ij*|Q_ij|^(n-1)         (hors-diagonale)
    ...
    return J  # symétrique, définie positive, creuse

def newton_raphson(H0, H_ref, demandes, r, eps_H=1e-4, max_iter=50):
    H = H0.copy()
    historique_residus = []
    for k in range(max_iter):
        F = residus(H, H_ref, demandes, r)
        J = jacobienne(H, H_ref, r)
        dH = np.linalg.solve(J, -F)   # Cholesky sparse en pratique
        H += dH
        residu = np.max(np.abs(dH))
        historique_residus.append(residu)
        if residu < eps_H:
            break
    return H, historique_residus
```

En production : `wntr.sim.EpanetSimulator` ou `wntr.sim.WNTRSimulator` (renvoi note **2.D** pour le point de fonctionnement des pompes).
:::

### La matrice de gradient : structure et propriétés

La jacobienne $\mathbf{J}$ est une matrice $N \times N$ à structure remarquable :

- **Symétrique et définie positive** : garantissent l'existence/unicité de la solution et permettent la décomposition de Cholesky — lien variationnel avec la forme quadratique d'énergie minimisée (note **0.2**).
- **Creuse** (*sparse*) : chaque nœud n'étant connecté qu'à un petit nombre de voisins, la quasi-totalité des coefficients hors diagonale sont nuls. Pour 10 000 nœuds, la matrice a ~50 000 coefficients non nuls au lieu de 100 millions.

$$a_{ij}^{(k)} = n \cdot r_{ij} \cdot |Q_{ij}^{(k)}|^{n-1} \qquad \text{(conductance hydraulique, recalculée à chaque itération)}$$

```{python}
#| label: fig-sparsity
#| fig-cap: "Motif de creux (*sparsity pattern*) de la matrice jacobienne $\\mathbf{J}$ pour le mini-réseau fil rouge (5 nœuds — matrice $5\\times5$) et pour un réseau de 50 nœuds (matrice $50\\times50$). Les points noirs représentent les coefficients non nuls. La structure est symétrique (définie positive) et creuse : chaque nœud ne se connecte qu'à ses voisins directs. Pour un réseau de 10 000 nœuds, la densité tombe à moins de 0,05 % — c'est cette sparsité qui rend la décomposition de Cholesky sparse 1 000× plus rapide qu'une inversion pleine."
import numpy as np
import matplotlib.pyplot as plt

C_CHARGE = "#1f4e79"

fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(9, 4.0))

# Mini-réseau 5x5
J5 = np.array([
    [ 3, -1, -1,  0, -1],
    [-1,  2,  0,  0, -1],
    [-1,  0,  2, -1,  0],
    [ 0,  0, -1,  2, -1],
    [-1, -1,  0, -1,  3],
], dtype=float)
ax1.spy(J5, markersize=14, color=C_CHARGE)
ax1.set_title("Matrice $5\\times5$ (mini-réseau fil rouge)\n$N_{nz} = 17/25 = 68\\,\\%$",
              fontsize=9.5)
ax1.tick_params(labelsize=8)

# Réseau 50x50 (connectivité type réseau maillé, ~3 voisins/nœud)
np.random.seed(42)
J50 = np.eye(50) * 4
for i in range(50):
    for j in np.random.choice([k for k in range(50) if k != i], 3, replace=False):
        J50[i, j] = -1; J50[j, i] = -1
ax2.spy(J50 != 0, markersize=4, color=C_CHARGE)
ax2.set_title("Matrice $50\\times50$ (réseau type)\n$N_{nz}\\approx 10\\,\\%$",
              fontsize=9.5)
ax2.tick_params(labelsize=8)

fig.suptitle("Motif de creux (sparsity pattern) — Matrice jacobienne $\\mathbf{J}$",
             fontsize=10.5)
fig.tight_layout()
plt.show()
```

### Convergence et critères d'arrêt

La convergence de Newton-Raphson sur un réseau bien conditionné est **quadratique** : le nombre de chiffres significatifs corrects double à chaque itération. En pratique, 10 à 20 itérations suffisent pour atteindre $10^{-6}$ m sur les charges.

Critères d'arrêt (EPANET) :

- **Résidu de débit** : $\max_i \left| \sum_{j} Q_{ij} - d_i \right| < \varepsilon_Q$ (typiquement $10^{-3}$ L/s)
- **Résidu de charge** : $\max_i \left| H_i^{(k+1)} - H_i^{(k)} \right| < \varepsilon_H$ (typiquement $10^{-4}$ m)

```{python}
#| label: fig-convergence
#| fig-cap: "Courbe de convergence de Newton-Raphson sur le mini-réseau fil rouge (résidu maximal de charge en échelle logarithmique, en fonction du numéro d'itération). La convergence est quadratique : la pente sur l'axe log est approximativement doublée à chaque itération — chaque pas divise le résidu par un facteur croissant. En 8 itérations, le résidu passe de $10^0$ m à $10^{-10}$ m. En comparaison, la courbe rouge montre un réseau mal conditionné (PRV avec consigne incohérente) qui oscille et ne converge pas en 20 itérations."
import numpy as np
import matplotlib.pyplot as plt

C_CHARGE = "#1f4e79"
C_PIEZO  = "#2e8b57"
C_ACCENT = "#c0392b"

iters = np.arange(1, 21)

# Convergence quadratique (résidu doublement logarithmique)
residu_ok = 10**(-(iters**1.8 * 0.55))
residu_ok = np.clip(residu_ok, 1e-12, 2)

# Cas mal conditionné : oscillation
residu_bad = 0.5 * np.abs(np.sin(iters * 1.1)) + 0.3 * np.exp(-iters * 0.05)

fig, ax = plt.subplots(figsize=(7.5, 4.0))
ax.semilogy(iters, residu_ok,  color=C_CHARGE, lw=2.5, marker="o", ms=5,
            label="Réseau bien conditionné (convergence quadratique)")
ax.semilogy(iters, residu_bad, color=C_ACCENT,  lw=2.0, marker="s", ms=5, ls="--",
            label="Réseau mal conditionné (PRV incohérent — non convergence)")

ax.axhline(1e-4, color="#aaa", lw=1.2, ls=":", label="Seuil $\\varepsilon_H = 10^{-4}$ m")
ax.axhline(1e-3/1000, color=C_PIEZO, lw=1.0, ls=":", alpha=0.6)

ax.set_xlabel("Numéro d'itération", fontsize=9)
ax.set_ylabel("Résidu maximal de charge (m)", fontsize=9)
ax.set_title("Convergence Newton-Raphson — Mini-réseau fil rouge", fontsize=10)
ax.legend(fontsize=8.5)
ax.set_xlim(1, 20)
ax.grid(alpha=0.2, which="both"); ax.tick_params(labelsize=8.5)
fig.tight_layout()
plt.show()
```

::: {.callout-note}
## Ce que fait EPANET à chaque itération — démystifier la boîte noire

À chaque pas d'itération, EPANET : (1) calcule les débits dans chaque arc à partir des charges courantes, (2) calcule les conductances hydrauliques $a_{ij}^{(k)}$, (3) assemble la matrice $\mathbf{J}$ et le vecteur résidu $\mathbf{F}$, (4) résout le système linéaire par décomposition de Cholesky sparse, (5) met à jour les charges nodales, (6) vérifie les critères de convergence. Ce cycle, incompréhensible vu de l'interface graphique, explique pourquoi un modèle mal conditionné (demandes inconsistantes, conduites de diamètre nul, PRV à consigne incohérente) ne converge pas — renvoi note **3.1** pour le calage, note **3.2** pour les limites du 1D.
:::

---

## Les conditions aux limites : ce qui ferme le système

### Nœuds à charge imposée : les réservoirs

Les réservoirs et châteaux d'eau sont les **points de référence** du modèle. Leur charge hydraulique $H = z_{plan d'eau}$ est connue et imposée — ce sont eux qui ancrent le niveau de pression de tout le réseau aval. Un réseau sans nœud à charge imposée est sous-déterminé. Le réservoir comme condition aux limites est fondé dans la note **0.3** (ouvrages et conditions aux limites).

### Nœuds à débit imposé : les demandes nodales

La demande $d_i$ est une donnée d'entrée — sa spatialisation et son foisonnement sont traités en note **1.A** ; cette note en utilise le résultat. La demande peut être fixe (hypothèse standard) ou **dépendante de la pression** (*PDD*), indispensable pour simuler des situations de pénurie :

$$d_i(H_i) = d_i^{\,\text{req}}\left(\frac{H_i - H_{min}}{H_{\,\text{ref}} - H_{min}}\right)^{1/2} \quad \text{pour } H_{min} \le H_i \le H_{\text{ref}}$$

```{python}
#| label: fig-pdd
#| fig-cap: "Loi de demande dépendante de la pression (PDD) : débit effectivement soutiré $d_i(H_i)/d_i^{req}$ en fonction de la charge disponible au nœud. En dessous de $H_{min}$ (ici 10 m), la demande est nulle (coupure de service). Entre $H_{min}$ et $H_{ref}$ (ici 30 m), la demande croît selon une loi racine carrée. Au-dessus de $H_{ref}$, la demande atteint son niveau requis. La demande fixe (pointillé rouge) est la limite $H_{ref} \\to H_{min}$. En situation de pénurie, utiliser la demande fixe surestime la quantité d'eau réellement disponible et sous-estime la durée de la perturbation de service."
import numpy as np
import matplotlib.pyplot as plt

C_CHARGE = "#1f4e79"
C_PIEZO  = "#2e8b57"
C_ACCENT = "#c0392b"

H = np.linspace(0, 50, 300)
H_min, H_ref = 10.0, 30.0

d_pdd = np.where(H < H_min, 0.0,
        np.where(H <= H_ref, ((H - H_min)/(H_ref - H_min))**0.5,
        1.0))

fig, ax = plt.subplots(figsize=(7.5, 4.0))
ax.plot(H, d_pdd, color=C_CHARGE, lw=2.5, label="PDD (dépendante de la pression)")
ax.axhline(1.0, color=C_ACCENT, lw=2.0, ls="--", label="Demande fixe (standard)")

ax.axvline(H_min, color="#aaa", lw=1.2, ls=":")
ax.axvline(H_ref, color="#aaa", lw=1.2, ls=":")
ax.text(H_min, -0.06, f"$H_{{min}}={H_min:.0f}$ m", ha="center", fontsize=8.5, color="#555")
ax.text(H_ref, -0.06, f"$H_{{ref}}={H_ref:.0f}$ m", ha="center", fontsize=8.5, color="#555")

ax.fill_between(H, 0, d_pdd, where=H < H_min, alpha=0.18, color=C_ACCENT,
                label="Coupure de service ($H < H_{min}$)")
ax.fill_between(H, d_pdd, 1.0, where=(H >= H_min) & (H <= H_ref),
                alpha=0.12, color=C_PIEZO, label="Déficit de service")

ax.set_xlabel("Charge disponible $H_i$ (m)", fontsize=9)
ax.set_ylabel("Demande relative $d_i(H_i)/d_i^{req}$", fontsize=9)
ax.set_title("Loi PDD — Demande dépendante de la pression (pénurie)", fontsize=10)
ax.legend(fontsize=8.5, loc="lower right")
ax.set_xlim(0, 50); ax.set_ylim(-0.1, 1.15)
ax.grid(alpha=0.2); ax.tick_params(labelsize=8.5)
fig.tight_layout()
plt.show()
```

### Pompes : la courbe H(Q) comme contrainte

Une pompe est un arc actif à conductance négative :

$$\Delta H_{pompe} = h_0 - r \cdot Q^n$$

Le point de fonctionnement réel résulte de l'intersection de la courbe caractéristique de la pompe et de la courbe de résistance du réseau — ce point est développé dans la note **2.D** (pompes et turbines) ; ici on en montre la résolution dans le contexte réseau.

```{python}
#| label: fig-point-fonctionnement
#| fig-cap: "Point de fonctionnement de la pompe du mini-réseau fil rouge : intersection de la courbe caractéristique de la pompe $H_p(Q) = h_0 - rQ^2$ (bleu) et de la courbe de résistance du réseau $H_{res}(Q)$ (vert, somme des pertes de charge sur le circuit). Le point de fonctionnement se déplace selon le niveau du réservoir aval : basses eaux (trait plein vert) → point B (débit plus élevé) ; hautes eaux (tirets verts) → point A (débit plus faible). Ce déplacement peut sortir la pompe de sa zone de rendement optimal (bande jaune) — vérification indispensable dans tout scénario EPS."
import numpy as np
import matplotlib.pyplot as plt
from scipy.optimize import brentq

C_CHARGE = "#1f4e79"
C_PIEZO  = "#2e8b57"
C_ACCENT = "#c0392b"

Q = np.linspace(0, 120, 300)
h0, r_p, n_p = 40.0, 0.0022, 2

def H_pompe(Q_): return h0 - r_p * Q_**2

R_res = 0.0015
def H_res_basses(Q_): return 20.0 + R_res * Q_**2
def H_res_hautes(Q_): return 28.0 + R_res * Q_**2

fig, ax = plt.subplots(figsize=(8, 4.2))
ax.plot(Q, H_pompe(Q), color=C_CHARGE, lw=2.5, label="Courbe pompe $H_p(Q)$")
ax.plot(Q, H_res_basses(Q), color=C_PIEZO, lw=2.0, label="Résistance réseau (basses eaux)")
ax.plot(Q, H_res_hautes(Q), color=C_PIEZO, lw=2.0, ls="--", label="Résistance réseau (hautes eaux)")

# Points de fonctionnement
for H_res, lbl, mk in [(H_res_basses, "B (basses eaux)", "o"),
                        (H_res_hautes, "A (hautes eaux)", "s")]:
    Q_fp = brentq(lambda q: H_pompe(q) - H_res(q), 1, 119)
    ax.scatter([Q_fp], [H_pompe(Q_fp)], color=C_ACCENT, s=90, zorder=7, marker=mk,
               label=f"Point {lbl[-16:]}: $Q={Q_fp:.0f}$ m³/h")

# Zone rendement optimal (BEP ± 15%)
Q_bep = 75
ax.axvspan(Q_bep*0.85, Q_bep*1.15, alpha=0.10, color="#f39c12", label="Zone rendement optimal")

ax.set_xlabel("Débit $Q$ (m³/h)", fontsize=9)
ax.set_ylabel("HMT (m)", fontsize=9)
ax.set_title("Point de fonctionnement pompe — Mini-réseau fil rouge", fontsize=10)
ax.legend(fontsize=8.5, loc="upper right", ncol=2)
ax.set_xlim(0, 120); ax.set_ylim(0, 45)
ax.grid(alpha=0.2); ax.tick_params(labelsize=8.5)
fig.tight_layout()
plt.show()
```

::: {.callout-important}
## Point de fonctionnement d'une pompe

Le point de fonctionnement réel d'une pompe résulte de l'intersection de sa courbe caractéristique et de la courbe de résistance du réseau. Ce point varie selon le niveau du réservoir aval, l'état des vannes et la demande instantanée. Le modèle le calcule à chaque itération — c'est une information critique pour vérifier que la pompe travaille dans sa plage de rendement optimal. Développement complet en note **2.D** (pompes et turbines en réseau).
:::

### Vannes et régulateurs de pression

Les PRV et PSV sont des conditions aux limites internes (note **0.3**). Un PRV impose une charge maximale en aval — si la charge calculée dépasse la consigne, la vanne se ferme partiellement. Cette logique de régulation est itérative par nature.

::: {.callout-warning}
## Alerte modélisation — Les PRV, première cause de non-convergence

Des consignes de PRV incohérentes (consigne aval supérieure à la charge disponible, ou deux PRV en série avec des consignes contradictoires) provoquent une oscillation ouverture/fermeture qui empêche la convergence. Symptôme : résidu oscillant sans décroître (cf. courbe de convergence). Diagnostic : vérifier d'abord la cohérence des consignes avant tout ajustement de rugosité.
:::

---

## Du régime permanent au régime étendu

### La boucle temporelle de l'EPS

La simulation en régime étendu (EPS) enchaîne des résolutions en régime permanent sur un horizon de 24 à 72 heures (renvoi notes **0.1** et **1.A** pour le fondement quasi-permanent). À chaque pas de temps :

1. Les demandes nodales sont mises à jour : $d_i(t) = d_i^{moy} \times f(t)$
2. Le niveau du réservoir est mis à jour :

$$z_{res}^{\,t+\Delta t} = z_{res}^{\,t} + \frac{\Delta t}{A_{res}}\big(Q_{in}^{\,t} - Q_{out}^{\,t}\big)$$

3. Le système est résolu par Newton-Raphson
4. Les résultats sont enregistrés et on passe au pas suivant

```{python}
#| label: fig-eps-cycle
#| fig-cap: "Sortie EPS sur 24 heures du mini-réseau fil rouge : coefficient de modulation de demande $f(t)$ (noir, axe gauche), niveau du réservoir R0 (bleu, axe droit) et état de la pompe (vert = active, rouge = arrêtée, figuré par la couleur de fond). La pompe s'arrête automatiquement quand le niveau dépasse 5,5 m et redémarre en dessous de 3 m (règles de contrôle). Le cycle journalier classique apparaît : remplissage nocturne (0 h–5 h), vidange en pointe matinale (6 h–9 h), remplissage partiel en creux de midi, vidange en pointe vespérale (18 h–21 h)."
import numpy as np
import matplotlib.pyplot as plt
from scipy.ndimage import gaussian_filter1d

C_CHARGE = "#1f4e79"
C_PIEZO  = "#2e8b57"
C_ACCENT = "#c0392b"

t = np.linspace(0, 24, 289)

# Courbe de modulation
f_mod = (0.6 + 0.9*np.exp(-((t-7.5)**2)/(2*1.2**2))
             + 0.4*np.exp(-((t-12.5)**2)/(2*1.5**2))
             + 0.6*np.exp(-((t-19.5)**2)/(2*1.8**2))
             - 0.25*np.exp(-((t-3.0)**2)/(2*2.0**2)))
f_mod = np.maximum(gaussian_filter1d(f_mod, sigma=5), 0.25)
Q_moy = 26.0  # m3/h total

# Niveau réservoir + état pompe (règle : on si < 3 m, off si > 5.5 m)
dt = t[1]-t[0]; A_res = 50.0
Q_prod_base = Q_moy * 1.05  # production nominale pompe active
z = np.zeros_like(t); z[0] = 4.5
pompe_on = np.ones_like(t, dtype=bool)
for i in range(1, len(t)):
    Q_prod = Q_prod_base if pompe_on[i-1] else 0.0
    z[i] = z[i-1] + (Q_prod - Q_moy*f_mod[i-1]) * dt / A_res
    z[i] = np.clip(z[i], 0, 7)
    if z[i] > 5.5: pompe_on[i] = False
    elif z[i] < 3.0: pompe_on[i] = True
    else: pompe_on[i] = pompe_on[i-1]

fig, ax1 = plt.subplots(figsize=(9, 4.2))
# Fond état pompe
for i in range(len(t)-1):
    col = "#eafaf1" if pompe_on[i] else "#fdf2f2"
    ax1.axvspan(t[i], t[i+1], alpha=0.5, color=col, linewidth=0)

ax1.plot(t, f_mod, color="k", lw=2.0, label="Coefficient de modulation $f(t)$")
ax1.set_xlabel("Heure (h)", fontsize=9)
ax1.set_ylabel("Coefficient de modulation", fontsize=9)
ax1.set_xlim(0, 24); ax1.set_ylim(0, 2.2)
ax1.tick_params(labelsize=8.5)

ax2 = ax1.twinx()
ax2.plot(t, z, color=C_CHARGE, lw=2.5, ls="--", label="Niveau réservoir R0 (m)")
ax2.axhline(5.5, color=C_PIEZO, lw=1.2, ls=":", label="Seuil arrêt pompe (5,5 m)")
ax2.axhline(3.0, color=C_ACCENT, lw=1.2, ls=":", label="Seuil démarrage pompe (3,0 m)")
ax2.set_ylabel("Niveau réservoir (m)", color=C_CHARGE, fontsize=9)
ax2.tick_params(axis="y", labelcolor=C_CHARGE, labelsize=8.5)
ax2.set_ylim(0, 8)

from matplotlib.patches import Patch
lines1,labs1 = ax1.get_legend_handles_labels()
lines2,labs2 = ax2.get_legend_handles_labels()
legend_extra = [Patch(fc="#eafaf1", ec="#27ae60", label="Pompe active"),
                Patch(fc="#fdf2f2", ec=C_ACCENT,  label="Pompe arrêtée")]
ax1.legend(lines1+lines2+legend_extra, labs1+labs2+[p.get_label() for p in legend_extra],
           fontsize=8, loc="upper right", ncol=2)
ax1.set_title("Cycle journalier EPS — Mini-réseau fil rouge (24 h)", fontsize=10)
ax1.grid(alpha=0.15)
fig.tight_layout()
plt.show()
```

### Les règles de contrôle : modéliser les automatismes

```sql
; Contrôle simple
LINK Pompe1 OPEN  IF NODE Reservoir1 BELOW 3.0
LINK Pompe1 CLOSED IF NODE Reservoir1 ABOVE 5.5

; Règle conditionnelle
RULE R1
IF   NODE Reservoir1 BELOW 2.0
AND  SYSTEM TIME >= 6:00
THEN LINK Pompe2 OPEN
PRIORITY 1
```

::: {.callout-tip}
## Pas de temps de l'EPS : le bon compromis

Un pas de temps trop long (> 1 h) rate les variations rapides et lisse les pointes. Un pas de temps trop court (< 5 min) multiplie le temps de calcul sans gain significatif pour un réseau en régime quasi-permanent. **15 minutes** est un bon compromis pour la plupart des études AEP.
:::

---

## Les scénarios de simulation : ce qu'on demande au modèle

### Scénario de pointe horaire

**Question :** les pressions restent-elles acceptables quand la consommation est maximale ?

$Q_{ph} = C_{ph} \times C_{pj} \times Q_{moy}$ (cascade des coefficients de pointe de la note **1.A**). Critère : $p \geq 1$ bar en tout nœud.

### Scénario de débit incendie

**Question :** le réseau peut-il fournir la demande de pointe + le débit incendie (60 m³/h, 2 h) avec une pression résiduelle $\geq 1$ bar ?

### Scénario de vannes fermées

**Question :** que se passe-t-il si on isole un tronçon pour travaux ?

### Scénario de pompe à l'arrêt

**Question :** la redondance de pompage est-elle suffisante en cas de panne ?

Ce scénario teste la résilience en **régime permanent** — il ne voit pas la dynamique transitoire de l'arrêt (coup de bélier, dépression). Pour la dynamique complète, renvoi note **2.A2** (l'angle mort transitoire de l'EPS, cf. note **0.1**).

```{python}
#| label: fig-lignes-piezo-scenarios
#| fig-cap: "Lignes piézométriques du mini-réseau fil rouge pour trois scénarios : nominal (bleu), pointe horaire (vert, demande ×2) et débit incendie au nœud N4 (rouge, +60 m³/h). Le seuil minimal de service (1 bar = 10 m de pression disponible, en supposant des cotes uniformes) est indiqué en tirets rouges. En débit incendie, le nœud N4 passe sous le seuil — il est le nœud critique. Ce graphique est la sortie-type d'un diagnostic AEP : identifier visuellement le tronçon limitant (chute de charge la plus forte) et le nœud critique (pression la plus faible)."
import numpy as np
import matplotlib.pyplot as plt

C_CHARGE = "#1f4e79"
C_PIEZO  = "#2e8b57"
C_ACCENT = "#c0392b"

# Charges simulées (valeurs indicatives) le long du chemin R0→N1→N2→N3→N4→N5
noeuds = ["R0", "N1", "N2", "N3", "N4", "N5"]
x_pos  = [0, 1, 2, 3, 4, 3]  # position relative pour le tracé

H_nominal  = [50, 47, 44, 40, 36, 38]
H_pointe   = [50, 44, 39, 33, 28, 31]
H_incendie = [50, 43, 38, 30, 22, 26]

cotes = [10, 10, 8, 9, 10, 9]  # cotes approximatives (m)
p_min_m = 10.0  # 1 bar = 10 m

fig, ax = plt.subplots(figsize=(8.5, 4.5))

# Terrains / conduites (schématique)
ax.fill_between(range(len(noeuds)), [c-2 for c in cotes], cotes,
                color="#c8a96e", alpha=0.4, label="Terrain (schématique)")
ax.plot(range(len(noeuds)), cotes, color="#c8a96e", lw=1.5)

# Seuil pression minimale
p_min_line = [c + p_min_m for c in cotes]
ax.plot(range(len(noeuds)), p_min_line, color=C_ACCENT, lw=1.8, ls="--",
        label="Seuil minimal ($p_{min}=1$ bar)")

# Lignes piézométriques
for H, col, ls, lbl in [(H_nominal,  C_CHARGE, "-",  "Nominal"),
                         (H_pointe,   C_PIEZO,  "--", "Pointe horaire (×2)"),
                         (H_incendie, C_ACCENT, "-.", "Incendie au nœud N4 (+60 m³/h)")]:
    ax.plot(range(len(noeuds)), H, color=col, lw=2.2, ls=ls, label=lbl)
    ax.scatter(range(len(noeuds)), H, color=col, s=40, zorder=5)

# Nœud critique (N4, indice 4)
ax.annotate("Nœud critique\n$p < 1$ bar", (4, H_incendie[4]),
            xytext=(3.3, 25),
            arrowprops=dict(arrowstyle="->", color=C_ACCENT),
            fontsize=8.5, color=C_ACCENT)

ax.set_xticks(range(len(noeuds))); ax.set_xticklabels(noeuds, fontsize=9.5)
ax.set_ylabel("Charge hydraulique $H$ (m NGF)", fontsize=9)
ax.set_title("Lignes piézométriques — Trois scénarios sur le mini-réseau fil rouge", fontsize=10)
ax.legend(fontsize=8.5, loc="upper right", ncol=2)
ax.set_ylim(0, 55)
ax.grid(alpha=0.2); ax.tick_params(axis="y", labelsize=8.5)
fig.tight_layout()
plt.show()
```

---

## Le calage sur mesures de terrain

### Pourquoi caler ?

Un modèle non calé est un outil de **prédimensionnement**, pas de diagnostic. Le calage ajuste les paramètres jusqu'à ce que les sorties simulées reproduisent les mesures de terrain avec une précision acceptable.

### Les données de terrain nécessaires

**Enregistreurs de pression** (24 à 72 h, pas de 5–15 min) ; **débitmètres de secteur** ; **débit de nuit minimum** (2 h–4 h — représente principalement les fuites, indicateur de pertes par secteur — renvoi note **1.A** pour la sectorisation, note **3.1** pour le calage avancé) ; **niveaux de réservoirs enregistrés** (bilan volumétrique direct).

### La procédure de calage pas à pas

**Phase 1 — Calage global :** vérifier d'abord la cohérence du bilan volumétrique. Un écart débit entrant/sortant indique un problème de données (vanne fermée non déclarée, conduite absente du SIG), pas de rugosité.

**Phase 2 — Calage local :** ajuster les coefficients de rugosité par zones homogènes pour minimiser l'écart pression simulée/mesurée.

::: {.callout-warning}
## Ne pas caler sur un seul instant

Un modèle calé uniquement sur un état de pointe peut être très mauvais en débit de nuit, et inversement. Le calage doit couvrir au moins **deux états contrastés** : pointe de consommation et débit minimal nocturne. Idéalement, caler sur 24 heures et valider sur une période indépendante.
:::

### Sensibilité du modèle et hiérarchie des paramètres

| Paramètre | Sensibilité sur les pressions | Sensibilité sur les débits |
|---|---|---|
| Rugosité des conduites principales | Forte | Faible |
| Rugosité des conduites secondaires | Faible | Faible |
| Demandes nodales | Forte | Forte |
| État des vannes | Très forte (binaire) | Très forte (binaire) |
| Charge des réservoirs | Forte | Moyenne |
| Courbe de pompe | Forte (aval pompe) | Forte |

: Sensibilité indicative des sorties du modèle aux paramètres d'entrée {#tbl-sensibilite}

```{python}
#| label: fig-tornado
#| fig-cap: "Diagramme tornado de sensibilité : impact relatif de chaque paramètre d'entrée sur la pression au nœud critique N4 (variation de ±20 % de chaque paramètre, toutes choses égales par ailleurs). L'état des vannes domine largement — une vanne partiellement fermée non déclarée peut provoquer un écart de plusieurs bars, bien plus grand que toute incertitude sur la rugosité. Ce diagramme justifie la séquence de calage : vérifier les vannes avant d'ajuster les rugosités."
import numpy as np
import matplotlib.pyplot as plt

C_CHARGE = "#1f4e79"
C_PIEZO  = "#2e8b57"
C_ACCENT = "#c0392b"

params = ["État des vannes", "Demandes nodales", "Charge réservoir",
          "Courbe de pompe", "Rugosité conduite principale",
          "Rugosité cond. secondaire"]
impact_pos = [4.8, 2.2, 1.8, 1.5, 0.9, 0.3]  # variation de pression (m)
impact_neg = [-5.2, -2.5, -1.6, -1.3, -0.7, -0.2]
colors = [C_ACCENT if abs(p) > 2.0 else C_CHARGE for p in impact_pos]

fig, ax = plt.subplots(figsize=(8.5, 4.0))
y = np.arange(len(params))
ax.barh(y, impact_pos, 0.55, color=colors, alpha=0.85)
ax.barh(y, impact_neg, 0.55, color=colors, alpha=0.55)
ax.axvline(0, color="k", lw=1.0)
ax.set_yticks(y); ax.set_yticklabels(params, fontsize=9)
ax.set_xlabel("Impact sur la pression au nœud critique N4 (m)", fontsize=9)
ax.set_title("Diagramme tornado — Sensibilité aux paramètres d'entrée (±20 %)", fontsize=10)
ax.grid(axis="x", alpha=0.2); ax.tick_params(labelsize=8.5)
fig.tight_layout()
plt.show()
```

### Indicateurs de qualité du calage

| Indicateur | Formule | Seuil acceptable |
|---|---|---|
| RMSE | $\sqrt{\frac{1}{n}\sum(H_{sim} - H_{mes})^2}$ | $< 2$ m pour la pression |
| Erreur relative moyenne | $\frac{1}{n}\sum\left\|\frac{H_{sim} - H_{mes}}{H_{mes}}\right\|$ | $< 5$ % |
| Nash-Sutcliffe (NSE) | $1 - \frac{\sum(H_{sim}-H_{mes})^2}{\sum(H_{mes}-\bar{H}_{mes})^2}$ | $> 0{,}85$ |
| Résidu de débit | $\left\|\sum Q_{ij} - d_i\right\|$ | $< 10^{-3}$ L/s |

: Indicateurs de qualité du calage pour un modèle AEP {#tbl-calage}

Ces indicateurs et le calage automatique par optimisation sont développés en note **3.1** (cohérence avec le NSE introduit en note **0.1**).

```{python}
#| label: fig-calage-nuage
#| fig-cap: "Nuage de points pression simulée vs pression mesurée en 12 points de mesure sur le mini-réseau fil rouge, après calage (phase 2). La droite $y=x$ représente le calage parfait. La bande jaune délimite l'écart $\\pm 2$ m (seuil RMSE acceptable). Deux points hors bande (croix rouges) indiquent des secteurs à revoir — ils correspondent aux zones à faible débit nocturne où les pertes modifient le régime par rapport aux hypothèses de spatialisation."
import numpy as np
import matplotlib.pyplot as plt

C_CHARGE = "#1f4e79"
C_PIEZO  = "#2e8b57"
C_ACCENT = "#c0392b"

np.random.seed(7)
H_mes = np.array([25, 28, 31, 22, 35, 38, 20, 32, 27, 40, 18, 30])
bruit = np.random.normal(0, 1.2, len(H_mes))
H_sim = H_mes + bruit
# 2 points hors bande
H_sim[3] += 3.5
H_sim[10] -= 3.2

ecart = H_sim - H_mes
dans_bande = np.abs(ecart) <= 2.0

fig, ax = plt.subplots(figsize=(6.5, 5.5))
h_min, h_max = 15, 45
ax.plot([h_min, h_max], [h_min, h_max], color="#aaa", lw=1.2, ls="--", label="$y = x$ (calage parfait)")
ax.fill_between([h_min, h_max], [h_min-2, h_max-2], [h_min+2, h_max+2],
                alpha=0.18, color="#f39c12", label="Bande $\\pm 2$ m")

ax.scatter(H_mes[dans_bande],  H_sim[dans_bande],  color=C_CHARGE, s=70, zorder=6,
           label="Dans la bande (calage OK)")
ax.scatter(H_mes[~dans_bande], H_sim[~dans_bande], color=C_ACCENT, s=90, zorder=7,
           marker="x", linewidths=2.5, label="Hors bande (à revoir)")

# RMSE
rmse = np.sqrt(np.mean(ecart**2))
ax.text(0.05, 0.95, f"RMSE = {rmse:.2f} m", transform=ax.transAxes,
        fontsize=9.5, color=C_CHARGE, va="top",
        bbox=dict(boxstyle="round,pad=0.3", fc="#f0f8ff", ec=C_CHARGE))

ax.set_xlabel("Pression mesurée (m)", fontsize=9)
ax.set_ylabel("Pression simulée (m)", fontsize=9)
ax.set_title("Nuage de points calage — Simulé vs mesuré", fontsize=10)
ax.legend(fontsize=8.5)
ax.set_xlim(h_min, h_max); ax.set_ylim(h_min, h_max)
ax.set_aspect("equal"); ax.grid(alpha=0.2); ax.tick_params(labelsize=8.5)
fig.tight_layout()
plt.show()
```

---

## Du modèle au diagnostic

### Identifier le tronçon limitant

Le modèle en pointe révèle les **goulots d'étranglement** : les tronçons où la perte de charge est la plus forte. La ligne piézométrique est l'outil classique (cf. figure scénarios ci-dessus) — un tronçon limitant se reconnaît à une chute de charge élevée sur une faible longueur et une pression aval proche du seuil.

### Détecter une anomalie de fonctionnement

Si les mesures de terrain s'écartent des simulations sur un secteur, cela indique une vanne partiellement fermée non déclarée, une conduite entartrée, ou un branchement illicite. Comparer le modèle calé à la réalité et interpréter les écarts est l'une des utilisations les plus précieuses du modèle en exploitation.

### Simuler un renforcement

```{python}
#| label: fig-renforcement
#| fig-cap: "Comparaison de la ligne piézométrique avant et après renforcement du tronçon limitant N1–N5 (doublement du diamètre DN 100 → DN 150). Avant renforcement (rouge), le nœud N4 est en sous-pression lors du scénario incendie. Après renforcement (bleu), la pression en N4 remonte au-dessus du seuil minimal. Le gain est de 8 m sur le nœud critique — obtenu pour un linéaire de conduite remplacé de 400 m. Ce type de comparaison avant/après est le cœur de l'analyse de variantes qui alimente les études de schéma directeur AEP."
import numpy as np
import matplotlib.pyplot as plt

C_CHARGE = "#1f4e79"
C_PIEZO  = "#2e8b57"
C_ACCENT = "#c0392b"

noeuds = ["R0", "N1", "N2", "N3", "N4", "N5"]
cotes  = [10, 10, 8, 9, 10, 9]
p_min  = [c + 10 for c in cotes]

H_avant  = [50, 43, 38, 30, 22, 26]
H_apres  = [50, 45, 41, 34, 30, 33]

fig, ax = plt.subplots(figsize=(8.5, 4.2))
ax.fill_between(range(len(noeuds)), [c-2 for c in cotes], cotes,
                color="#c8a96e", alpha=0.4)
ax.plot(range(len(noeuds)), cotes, color="#c8a96e", lw=1.5)
ax.plot(range(len(noeuds)), p_min, color=C_ACCENT, lw=1.8, ls="--",
        label="Seuil minimal (1 bar)")
ax.plot(range(len(noeuds)), H_avant, color=C_ACCENT, lw=2.2, marker="s", ms=7,
        label="Avant renforcement (scénario incendie)")
ax.plot(range(len(noeuds)), H_apres, color=C_CHARGE, lw=2.5, marker="o", ms=7,
        label="Après renforcement (DN 100→150 sur N1–N5)")

# Flèche gain N4
ax.annotate("", xy=(4, H_apres[4]), xytext=(4, H_avant[4]),
            arrowprops=dict(arrowstyle="<->", color=C_PIEZO, lw=2.0))
ax.text(4.18, (H_apres[4]+H_avant[4])/2, f"+{H_apres[4]-H_avant[4]} m",
        fontsize=9, color=C_PIEZO)

ax.set_xticks(range(len(noeuds))); ax.set_xticklabels(noeuds, fontsize=9.5)
ax.set_ylabel("Charge hydraulique $H$ (m NGF)", fontsize=9)
ax.set_title("Avant / après renforcement — Scénario incendie, nœud critique N4", fontsize=10)
ax.legend(fontsize=8.5, loc="lower left")
ax.set_ylim(0, 55)
ax.grid(alpha=0.2); ax.tick_params(axis="y", labelsize=8.5)
fig.tight_layout()
plt.show()
```

---

## Conclusion

La résolution hydraulique d'un réseau AEP en régime permanent n'est pas une boîte noire mystérieuse — c'est l'application systématique de Newton-Raphson à un système d'équations de conservation sur un graphe représentant la topologie du réseau. Comprendre ce mécanisme permet de diagnostiquer les problèmes de convergence, d'interpréter les résultats avec discernement, et de concevoir des scénarios de simulation pertinents.

La valeur opérationnelle du modèle repose sur deux piliers : la **rigueur du calage** (un modèle non calé est indicatif, pas prédictif) et la **pertinence des scénarios** (un bon modèle mal interrogé ne répond pas aux bonnes questions). Ces deux piliers sont entre les mains de l'ingénieur, pas du logiciel.

---

## Pour aller plus loin dans cette série

**Fondements**

- **Note 0.1** — *Généralités sur la modélisation* : cascade dimensionnelle, méthode du gradient hydraulique, régimes permanent et transitoire — prérequis direct de cette note
- **Note 0.2** — *Énergie, charge et dégradation* : fonde la loi des mailles et la notion de résistance hydraulique $r_{ij}$
- **Note 0.3** — *Ouvrages et conditions aux limites* : réservoirs, pompes, PRV comme conditions aux limites — base du bien-posé du système
- **Note 0.4** — *Analyse dimensionnelle* : Moody, Colebrook — fondent les résistances $r_{ij}$ et l'exposant $n$

**Entrée métier**

- **Note 1.A** — *Modéliser un réseau AEP* : contexte métier, demande, foisonnement, sectorisation — données d'entrée de cette note

**Applications**

- **Note 2.A2** — *Coups de bélier* : la dynamique transitoire que le régime permanent ne voit pas
- **Note 2.D** — *Pompes et turbines* : point de fonctionnement développé, VFD, associations de pompes

**Transversales**

- **Note 3.1** — *Calage et validation* : optimisation automatique, sensibilité formelle, indicateurs avancés
- **Note 3.2** — *Quand le 1D ne suffit plus* : limites du modèle réseau, CFD pour les ouvrages ponctuels
- **Note 3.3** — *Qualité de l'eau* : le moteur hydraulique résolu ici porte le transport de solutés (âge de l'eau, chlore)
- **Note 3.6** — *Limites et responsabilité* : documenter les hypothèses, traçabilité des calages
